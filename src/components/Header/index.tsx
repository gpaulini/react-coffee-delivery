import { HeaderContainer } from './styles'

export const Header = () => {
  return (
    <HeaderContainer>
      <span>Logo</span>
      <span>Options and settings...</span>
    </HeaderContainer>
  )
}
